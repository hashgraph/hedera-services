// SPDX-License-Identifier: Apache-2.0
package com.hedera.services.bdd.suites.utils.validation.domain;

/**
 * This scenario validates that every node in the network can be used as an account's stake id.
 *
 * <p>It does not (currently) require any peristent state to be set up.
 */
public class StakingScenario {
    // Do we need anything at all?
}
